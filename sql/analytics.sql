SELECT
	CATEGORY,
	SUM(AMOUNT) AS TOTAL_SALES
FROM
	SALES_DATA
GROUP BY
	CATEGORY;

SELECT
	CUSTOMER_NAME,
	SUM(AMOUNT) AS TOTAL_AMOUNT
FROM
	SALES_DATA
GROUP BY
	CUSTOMER_NAME
ORDER BY
	TOTAL_AMOUNT DESC;